import { cva } from "class-variance-authority";

export const container = cva("relative");

export const icon = cva("text-muted-foreground absolute left-2 top-2.5 h-4 w-4");

export const input = cva("pl-8");
