import { PostPaginationProps } from "../../../entities/types";
import { 
  Select,
  SelectTrigger,
  SelectValue,
  SelectContent,
  SelectItem,
  Button 
} from "../../../shared/ui";

export const PostPagination = () => {
  
};